# Kept for backward compatibility - utilities are now integrated into targets.py
# This module is no longer needed with Playwright but kept to avoid import errors
# if any external code references it.
