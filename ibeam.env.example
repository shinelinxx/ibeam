### IBKR 账户凭据 ###
IBEAM_ACCOUNT=你的IBKR用户名
IBEAM_PASSWORD=你的IBKR密码
# IBEAM_KEY=可选的 Fernet 加密密钥

### 两步验证（仅在账户开启了 2FA 时需要配置） ###
### 如果你的账户未开启 2FA，请注释掉或删除以下配置。
###
### IBEAM_TWO_FA_HANDLER 可选项：
###
###   TOTP — 基于时间的一次性密码（全自动，推荐）
###     IBEAM_TOTP_SECRET   = Base32 编码的密钥（必填，从 IBKR 账户管理 -> 安全登录系统 获取）
###     IBEAM_TOTP_DIGITS   = TOTP 位数（默认 6）
###     IBEAM_TOTP_PERIOD   = TOTP 刷新间隔秒数（默认 30）
###
###   GOOGLE_MSG — 通过 Google Messages 网页版读取 IB 短信验证码
###     无额外参数，需要 Google Messages 已绑定手机并开启网页版
###
###   NOTIFICATION_RESEND — 点击"重新发送通知"，等待用户在 IBKR App 手动确认
###     无额外参数
###
###   EXTERNAL_REQUEST — 调用外部 HTTP 接口获取验证码
###     IBEAM_EXTERNAL_REQUEST_URL     = 请求 URL（必填）
###     IBEAM_EXTERNAL_REQUEST_METHOD  = 请求方法（默认 GET）
###     IBEAM_EXTERNAL_REQUEST_TIMEOUT = 超时秒数（默认 300）
###     IBEAM_EXTERNAL_REQUEST_PARAMS  = URL 参数，JSON 格式（可选）
###     IBEAM_EXTERNAL_REQUEST_DATA    = POST 数据，JSON 格式（可选）
###     IBEAM_EXTERNAL_REQUEST_HEADERS = 请求头，JSON 格式（可选）
###
###   CUSTOM_HANDLER — 自定义 Python Handler
###     IBEAM_CUSTOM_TWO_FA_HANDLER = 模块名.类名（默认 custom_two_fa_handler.CustomTwoFaHandler）
###     需将 .py 文件放入 inputs 目录（默认 /srv/inputs/）
###

# IBEAM_TWO_FA_HANDLER=TOTP
# IBEAM_TOTP_SECRET=你的Base32密钥

### 2FA 设备选择（多个认证设备时需要配置） ###
### 当 IB 登录页出现 "Select Second Factor Device" 下拉框时使用
### 默认值为 "Mobile Authenticator App"，可改为 "IB Key" 等
# IBEAM_TWO_FA_SELECT_TARGET=Mobile Authenticator App

### 网关配置 ###
# IBEAM_GATEWAY_BASE_URL=https://localhost:5000
# IBEAM_LOG_LEVEL=INFO
# IBEAM_ERROR_SCREENSHOTS=True
# IBEAM_PAGE_LOAD_TIMEOUT=30
